/**
 * This is the central place to export all page spacific components
 */

export const FilterContainer = require('./filterContainer').default
export const ImageContainer = require('./imageContainer').default
