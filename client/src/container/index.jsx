/**
 * It is the central place for exporting all the necessary containers
 */

export const QuickSearch = require('./quickSearch').default
export const ResturantDetails = require('./restutantDetails').default
export const SearchPlaces = require('./searchPlaces').default
